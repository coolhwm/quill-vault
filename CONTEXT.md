# Quillvault

Quillvault 是面向个人用户的本地优先会议纪要产品。本词汇表统一项目说明、设计与验证过程中使用的核心业务语言。

## Language

**能力/期权型项目**：
以完成产品上架、积累可复用能力并验证真实用户需求为首要目标的项目；收入是次要结果，不以短期盈利作为继续投入的前提。
_Avoid_: 收入型项目、创业项目

**PKM 技术用户**：
使用 iPhone 与 Obsidian/Markdown、能够配置 API Key，并希望自主掌握会议文件的个人用户；是首发版本唯一核心用户。
_Avoid_: 普通职场用户、企业客户、所有会议用户

**面对面会话**：
参与者处于同一物理空间，由 iPhone 麦克风采集的会议、讨论或访谈；不包含捕获其他 App 系统音频的在线会议。
_Avoid_: 在线会议、通话录音、会议机器人

**快速开始**：
从 App 首页点击一次主按钮，或由用户预先绑定 Action Button 后启动面对面会话记录；不包含 Siri、锁屏组件或 Apple Watch 入口。
_Avoid_: 一键启动、全场景快捷录音

**设备端纪要**：
在兼容并已启用 Apple Intelligence 的设备上，由系统端侧模型从本地逐字稿生成的纪要；音频、逐字稿和纪要均不离开设备，是默认纪要模式。
_Avoid_: 本地转写、离线转写

**BYOK 纪要**：
用户配置 OpenAI 兼容接口的 Base URL、Model 与 API Key 后，将逐字稿文本直达该服务商生成的纪要；不发送音频，也不经过 Quillvault 服务器。连接测试必须实际请求模型并验证其能返回纪要所需的结构化数据，不能只检查网络或 HTTP 状态；API Key 仅存于系统 Keychain，不适配 Anthropic、Gemini 等非兼容协议。
_Avoid_: 完全本地纪要、Quillvault 云端纪要

**纪要兼容设备**：
运行 iOS 26 或更高版本，且支持并已启用 Apple Intelligence 的 iPhone；可使用设备端纪要。不满足条件的 iOS 26+ iPhone 仍可本地实时转写，但生成纪要需要 BYOK。
_Avoid_: 支持设备、iOS 26 设备

**结构化纪要**：
一次面对面会话结束后生成的固定产物，包含会议总览、带时间范围的章节纪要、关键决策、带负责人/截止时间/原文依据的待办、风险与未决问题、核心观点图、按需生成的章节图，以及可按时间回溯的完整逐字稿。
_Avoid_: 飞书式纪要、AI 摘要、会议总结

**两级图示**：
结构化纪要始终包含一张概括整场会话核心观点与关系的 Mermaid 核心观点图；适合以流程、时序、分支或多观点关系表达，且图示比文字列表更清晰的章节，可在对应段落附加 Mermaid 章节图，每份纪要最多三张章节图。
_Avoid_: 单图纪要、每段配图、AI 自由制图

**权威目录**：
用户会议资产的唯一真实存储位置。默认是 Quillvault 的 iCloud Drive 目录；用户选择 Obsidian Vault 子目录后，该目录直接成为权威目录。Quillvault 不在另一位置维护镜像副本，也不承担双向同步与冲突合并。
_Avoid_: 导出到 Obsidian、Obsidian 副本、双向同步

**会议资产**：
一次面对面会话产生并写入权威目录的原始录音、完整逐字稿和结构化纪要。用户可在设置中关闭“纪要生成后保留原始录音”，但默认开启；关闭时仅在逐字稿与纪要成功写入后删除录音，处理中或生成失败时必须保留。
_Avoid_: 云端会议记录、仅 AI 摘要、临时录音

**后台记录**：
面对面会话进入锁屏或 App 后台后，原始录音必须持续且不中断。实时转写在前台持续显示；若 iOS 暂停后台分析，Quillvault 在恢复前台或结束会话后从原始录音自动补齐逐字稿，不承诺不可见期间逐秒完成转写。
_Avoid_: 后台实时转写保证、仅前台录音

**待确认负责人**：
当会话原文没有明确说出待办负责人的姓名时，结构化纪要必须使用的负责人状态。v1 不做自动说话人区分，不使用“说话人 1/2”伪装身份识别，也不得根据声音或发言顺序猜测真实姓名；待办仍需附原文依据。
_Avoid_: 未知说话人、AI 推测负责人、自动声纹识别

**会议资产目录**：
每场会话对应的独立文件夹，至少包含带 YAML Front Matter 和内嵌 Mermaid 的 `minutes.md`、按时间戳分段的 `transcript.md`，以及按用户设置决定是否保留的 `recording.m4a`。Markdown 文件是内容真相；App 内数据库只可用于能够从这些文件重建的本地索引。
_Avoid_: 私有笔记数据库、不可迁移项目文件、数据库导出 Markdown

**绝对隐私边界**：
Quillvault 不集成统计、广告或第三方崩溃上报 SDK，不上传使用事件、设备标识、录音、逐字稿或纪要；除系统 iCloud 同步和用户主动发起的 BYOK 请求外，不产生业务网络请求。产品验证仅通过用户主动反馈、访谈与 TestFlight 问卷进行。
_Avoid_: 匿名遥测、隐私友好分析、可选崩溃上报

**永久解锁**：
用户可免费完整体验三场、每场最多十五分钟的面对面会话；之后通过非消耗型 App Store 内购，以首发价 ¥38 / US$4.99 一次性解锁全部现有功能。Quillvault 不提供订阅层级；BYOK 模型费用由用户直接支付服务商。
_Avoid_: 免费版、订阅会员、按分钟付费

**四周验证门槛**：
TestFlight 发布后四周内，招募十五位 PKM 技术用户；至少十位用于一次真实面对面会议、六位累计使用三次以上、五位将会议资产保留在 Obsidian 或 iCloud 中继续使用、三位实际完成永久解锁。结果通过用户主动反馈、访谈记录与购买结果核验；未达到时先定位问题，不直接扩大功能范围。
_Avoid_: 下载量目标、埋点漏斗、主观感觉成功

**音频导入**：
从系统“文件”中选择单个已有音频，并生成与现场记录相同的会议资产；是 v1 的次要入口。v1 不支持视频、相册音轨提取、批量导入或分享扩展，也不将产品定位扩展为通用音视频转写工具。
_Avoid_: 媒体导入、批量转写、视频纪要

**技术可行性闸门**：
完整功能开发前执行的五天真机 Spike。必须验证 Action Button 冷启动录音、中文会议连续录制六十分钟且后台音频无丢失并可补齐逐字稿、长逐字稿分层生成结构化纪要与 Mermaid、重启后继续写入权威目录，以及 Mermaid 完全离线渲染。未通过的核心项必须先调整范围或路线；六至八周功能开发周期从闸门通过后开始。
_Avoid_: 技术预研、后续优化、边做边验证

**真机技术闭环 Demo**：
在 iOS 26 真机上实际跑通面对面会话录音、设备端实时转写、BYOK 配置与密钥加密存储、BYOK 结构化纪要、确定性 Mermaid，以及 Markdown 会议资产落盘的抛弃式原型；BYOK 是纪要生成的验收路径，设备端纪要不作为 Demo 通过条件。它只用于验证核心链路，不作为正式产品的工程基础。
_Avoid_: 交互原型、界面 Demo、完整 v1、生产版本骨架

**验证语言**：
v1 完整测试并对质量作出承诺的简体中文与英语。技术实现不写死语言列表，而是根据 `SpeechTranscriber.supportedLocales` 与 `installedLocales` 动态展示设备可用语言并按需下载模型；其他系统支持语言作为实验性能力，不承诺混合语言识别或会中自动切换。
_Avoid_: 仅支持中英文、支持所有语言、多语言混说
