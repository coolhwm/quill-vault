---
status: accepted
---

# 使用 GRDB 维护可重建索引与任务状态

Quillvault MVP 使用 GRDB 管理 SQLite 数据库，保存可从权威目录重建的会议与全文检索索引，以及可恢复纪要任务的检查点和脱敏诊断元数据；会议正文与录音仍以 Markdown 和音频文件为唯一内容真相。相较 SwiftData，GRDB 提供更显式的 schema 迁移、事务、WAL 并发与 FTS 控制，适合长期演进的任务队列和高强度会议检索。数据库损坏或丢失不得导致会议资产丢失，应用必须支持从权威目录重建内容索引，并依据仍可验证的文件状态恢复或安全重建未完成任务。
